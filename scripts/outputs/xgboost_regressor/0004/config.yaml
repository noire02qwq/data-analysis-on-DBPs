model:
  type: XGBOOST
  name: xgboost_regressor/tmp_reg_lambda_4e3ceb
  history_length: 1
  max_depth: 8
  learning_rate: 0.1
  subsample: 0.95
  colsample_bytree: 0.8
  gamma: 0.0
  reg_lambda: 1.5
  min_child_weight: 3
training:
  max_epochs: 600
  batch_size: 0
  learning_rate: 0.001
  weight_decay: 0.0
  checkpoint_interval: 10
  seed: 2023
data:
  input_csv: data/time_aligned_data.csv
  timestamp_column: Date, Time
