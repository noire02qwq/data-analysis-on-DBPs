现在除了gru所有模型的四个自动调参脚本都已经跑完，请先根据原来的test程序编写新test程序，分为值回归和rate回归，trc和其他值四个程序，输出真实值和预测值的csv，以及可视化，和以前的test程序一样。每个test程序还要额外再生成一个csv表格，每行代表一个预测值，比如trc就是两行trc-ppl1, trc-ppl2，每列代表一个统计值，除了第一列显示预测值名称后面分别需要r2，mse，rmse，mae。

更新并编写完程序后，对所有模型（mlp mlphis lstm rnn）每种模式（trc/other, 值回归/rate回归）共16个实验，每个实验读日志选取best val loss前三优秀的trial运行test，日志是根目录下的那些.log文件，由根目录的两个sh脚本产生。对这些test结果再编写三个横向对比csv（直接生成csv而不是py程序），每个表格对应一个统计的代表值分别是trc的trc-ppl2, other的ph-ppl2 toc-ppl2，每行代表一次test，其中第一列是模型-模式-trial编号，比如lstm-rate-trial01（对这列的形式举例而不是实际内容），后面的列是r2 mse rmse mae