model:
  type: XGBOOST
  name: xgboost_regressor/tmp_gamma_f8a137
  history_length: 1
  max_depth: 8
  learning_rate: 0.08
  subsample: 0.95
  colsample_bytree: 0.75
  gamma: 0.2
  reg_lambda: 1.5
  min_child_weight: 3
training:
  max_epochs: 600
  batch_size: 0
  learning_rate: 0.001
  weight_decay: 0.0
  checkpoint_interval: 10
  seed: 2023
data:
  input_csv: data/time_aligned_data.csv
  timestamp_column: Date, Time
